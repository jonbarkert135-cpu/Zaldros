[project]
name = "bedrock-compat"
version = "0.1.0"
description = "Compatibility registries with evidence enforcement for Bedrock Linux"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
bedrock-compat = "bedrock_compat.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
