[project]
name = "bedrock-bench"
version = "0.1.0"
description = "Baseline/compare performance harness for Bedrock OS"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
bedrock-bench = "bedrock_bench.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
