[project]
name = "zaldros-hwinfo"
version = "0.1.0"
description = "Real hardware and system inventory for Zaldros OS"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
zaldros-hwinfo = "zaldros_hwinfo.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
