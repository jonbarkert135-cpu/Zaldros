[project]
name = "zaldros-sysprobe"
version = "0.1.0"
description = "Service, dependency and resource map generator for Zaldros OS"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
zaldros-sysprobe = "zaldros_sysprobe.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
