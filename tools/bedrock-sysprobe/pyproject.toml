[project]
name = "bedrock-sysprobe"
version = "0.1.0"
description = "Service, dependency and resource map generator for Bedrock OS"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
bedrock-sysprobe = "bedrock_sysprobe.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
