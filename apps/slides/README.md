# Zaldros Slides

Окно в форме PowerPoint на движке LibreOffice Impress (ADR-0022, продолжение ADR-0013).

`python -m zaldros_slides render --out slides.png [--open deck.pptx]`. Нужны
`libreoffice-impress-nogui` и `python3-uno`.
